# Convenience shim — the canonical dependency list lives in pyproject.toml.
# The "local" extra adds llama-cpp-python for GGUF inference
# (python -m tmc_llm.cli / python -m tmc_llm.api).
-e .[local]
